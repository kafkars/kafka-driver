#!/usr/bin/env bash
set -euo pipefail

cargo test --release --package kafka-driver \
  authentication::scram::fanout_bench_test::scram_proof_fanout_profile \
  -- --exact --ignored --nocapture --test-threads=1
